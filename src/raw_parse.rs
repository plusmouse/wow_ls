#![allow(bad_style, unused)]
#[repr(u16)]
pub enum SyntaxKind {
    //Punctuation
    PLUS,
    MINUS,
    ASTERISK,
    SLASH,
    MODULO,
    HAT,
    HASH,
    EQUALS_BOOLEAN,
    NOT_EQUALS,
    LESS_THAN_EQUALS,
    GREATER_THAN_EQUALS,
    LESS_THAN,
    GREATER_THAN,
    ASSIGN_EQUAL,
    L_BRACKET,
    R_BRACKET,
    L_CURLY_BRACKET,
    R_CURLY_BRACKET,
    L_SQUARE_BRACKET,
    R_SQUARE_BRACKET,
    SEMICOLON,
    COLON,
    COMMA,
    ONE_DOT,
    TWO_DOTS,
    THREE_DOTS,
    // Keywords
    AND,
    BREAK,
    DO,
    ELSE,
    ELSEIF,
    END,
    FALSE,
    FOR,
    FUNCTION,
    IF,
    IN,
    LOCAL,
    NIL,
    NOT,
    OR,
    REPEAT,
    RETURN,
    THEN,
    TRUE,
    UNTIL,
    WHILE,
    //Literals
    STRING,
    NUMBER,
    TABLE_INIT,
    //System
    CHUNK,
    STAT,
    VAR_LIST,
    EXP_LIST,
    ASSIGN_STAT,
    LOCAL_STAT,
    WHILE_STAT,
    REPEAT_STAT,
    IF_STAT,
    RETURN_STAT,
    BREAK_STAT,
    FOR_STAT_COUNTER,
    FOR_STAT_ITER,
    FUNCTION_STAT,
    //Expressions
    EXP,
    PREFIX_EXP,
    //...
}
